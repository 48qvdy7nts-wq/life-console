{
  "name": "portfolio-site",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync:apple": "node scripts/sync_apple.mjs",
    "sync:local": "node scripts/scan_local_sources.mjs",
    "sync:projects": "node scripts/sync_projects.mjs",
    "sync:all": "npm run sync:apple && npm run sync:local && npm run sync:projects",
    "build": "vite build",
    "build:all": "npm run sync:all && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "framer-motion": "^10.16.0",
    "tailwindcss": "^3.3.0",
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.32"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
